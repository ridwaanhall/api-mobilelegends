[project]
name = "mlbb"
version = "3.2.3"
requires-python = ">=3.14"

dependencies = [
    "fastapi[standard]>=0.135.2",
    "requests==2.33.0",
    "cryptography==46.0.7",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
